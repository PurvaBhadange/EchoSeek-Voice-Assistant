services:
  - type: web
    name: echoseek-backend
    env: python
    region: singapore
    buildCommand: pip install -r backend/requirements.txt && python scripts/prepare_dataset_subset.py && python scripts/generate_embeddings.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: GEMINI_API_KEY
        sync: false
      - key: SARVAM_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: '["*"]'
